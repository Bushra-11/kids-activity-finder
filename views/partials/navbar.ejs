<nav>
    <a href="/">Homepage</a>
    <a href="/activities">All Activities</a>
  
    <% if (user && user.role === 'provider') { %>
      <a href="/activities/new">Create Activity</a>
      <a href="/enrollments">View Enrollment</a>
    <% } %>

    <% if (user && user.role === 'parent'){ %>
      <a href="/enrollments">Enrollment History</a>
      <a href="/activities/recommendations">Recommended for You</a>
      <a href="/favorites">Favorites</a>
      <% } %>

    <% if (user) { %>
      <a href="/auth/sign-out">Logout</a>
    <% } else { %>
      <a href="/auth/sign-up">Sign up</a>
      <a href="/auth/sign-in">Login</a>
    <% } %>
</nav>